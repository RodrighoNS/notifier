<%= form_with(model: email_channel) do |form| %>
  <% if email_channel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(email_channel.errors.count, "error") %> prohibited this email_channel from being saved:</h2>

      <ul>
        <% email_channel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
